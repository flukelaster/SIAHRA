node_modules/
dist/
.wrangler/
*.log
.DS_Store
apps/etl/data/raw
apps/etl/data/work
.env
.env.local
# per-worktree dev ports (scripts/setup-worktree.sh)
.env.worktree
# คีย์ R2 S3 สำหรับ scripts/upload-tiles.sh
scripts/.env.r2
.playwright-cli/
# Native-resolution terrain tile pyramids (GBs, ~100k files) — regenerated by
# `npm run build:tiles -w apps/etl`, served in dev by a Vite middleware and
# meant for R2 in production, not the git repo or the Worker asset bundle.
apps/etl/data/tiles
