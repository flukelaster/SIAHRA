# ค่าที่ Vite อ่านเฉพาะตอน `vite` dev (mode development) — build production ไม่อ่านไฟล์นี้
# E15: ชั้นภาพกล้อง CCTV ของกรมทรัพยากรน้ำ เปิดใน dev เท่านั้น จนกว่า DWR จะอนุญาต
# (ดู src/lib/featureFlags.ts และ apps/etl/src/build-cctv.README.md)
VITE_FEATURE_CCTV=1
